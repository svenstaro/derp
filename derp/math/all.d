/**
Module wrapping all math stuff, so we are able to replace gl3n at some point and add simd support
*/
module derp.math.all;

public import 
    std.math,
    derp.math.angle,
    derp.math.vector,
    derp.math.matrix,
    derp.math.quaternion;
    

